# Written automatically by `npx convex dev` for local or cloud development.
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=
VITE_WORKOS_CLIENT_ID=
VITE_WORKOS_REDIRECT_URI=http://localhost:5173/callback

# Set only in the protected Convex deployment, never in this file or Vite:
# WORKOS_API_KEY, PILOT_ROSTER_JSON, and GOOGLE_GEMINI_API_KEY.
# WORKOS_CLIENT_ID is also set in Convex so the backend can validate JWTs.
# PILOT_ROSTER_JSON uses {"version":1,"entries":[{"email":"...","institutionKey":"umb","roles":["learner"]}]}.
# npx convex env set GOOGLE_GEMINI_API_KEY <value>
